words ← ⊃⎕NGET'wordlewordles'1

⍝ 's?i??' q words
q ← {(,×/⍉↑{((1⌷⍵)∊'?',⊢)¨(1↓⍵)}¨↓⍺,⍉↑⍵)/⍵}

⍝ 's?i??' '?k???' batchq words
batchq ← {↑q/(⌽⍺),⊂⍵}

green ← {(,×/⍉↑{((1⌷⍵)∊'?',⊢)¨(1↓⍵)}¨↓⍺,⍉↑⍵)/⍵}
yellow ← {(((('?'≠⍺)/⍺)(×⌿∘.∊)⍵)∧(~,×/⍉↑{((1⌷⍵)∊'?',⊢)¨(1↓⍵)}¨↓⍺,⍉↑⍵))/⍵}
grey ← {(~(⍺(×⌿∘.∊)⍵))/⍵}
